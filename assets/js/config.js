/* ============================================================
   太阳花守护官网 - 全局配置
   ------------------------------------------------------------
   WORKER_URL：Cloudflare Worker 部署地址（部署后填入）
   版本号硬编码在 HTML 中（JSON-LD / SEO 需要静态文本）。
   ============================================================ */
var SG_CONFIG = {
  WORKER_URL: ''
};
